# Checkpoints

This folder is reserved for small local MLP Chief of Staff checkpoints.

Large LLM adapters and model weights are intentionally not committed. The
replayable judging evidence lives in `training/evidence/`, and re-runnable
training scripts live in `training/scripts/`.
