# Demo script

- Start on the ablation slide.
- Run `python inference.py --oracle --task medium_brief`.
- Point out the expert sequence: analyst -> finance -> strategy -> hr -> summarize -> submit.
- Show `training/reward_curves/reward_curve.png`.
- End on why multi-agent routing beats a flat single baseline.
